
// this can be either .json or .xml, or we can make it a .jsx file with an object inside (for convenience)
// Here all the information about the location of required endpoints should be (like ip,port , etc);
// If, after all, we will use a dispatcher endpoint, this file should be relatively small.
